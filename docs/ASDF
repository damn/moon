First all in one FOLDER
each form

then too many (>7? )
=> create sub-folders

then just ask the LLM to create more sub-folders or simplify
  !!!

    => the LLM sees the whole context ...

      => so we have to reduce the context of the project
          => by making it smaller and creating open source libraries (=> ALSO GOOD IDEA !)

            => anything can be, no documentation,readme required, read the code!

              => every day 1 library??

              can even be libgdx with just 1 form???

                => Split libgdx #1 problem !!!


                => LANGUAGE MODEL CREATE LANGUAGE FOR PROGRAMMING
