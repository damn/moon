-> bring libgdx concepts into clojure
    clojure.orthographic-camera
    clojure.batch
    clojure.sprite-batch
    clojure.button
    clojure.layout
    clojure.actor
    clojure.stage
    clojure.group

    etc. ! why not

    only clojure.gdx = gdx ...!


    just _names_
    => realease as 'clojure.gdx' game development extension for clojure
        (only implementation ATM == com.badlogigc.gdx)
