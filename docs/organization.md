  I have an idea - first step we have all files in clojure/ folder

  Next step we make a folder for each file - because its too many files

  Then if we have too many folders we make each folder into a sub-folder

  So we have same level of nesting for everything - maximum unique hops for each thing
