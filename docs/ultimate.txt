# Ultimate Clojure Code Organizational Principle
          -- UCCOP - -

* Every namespace is 'clojure.noun.verb'
* No sub-folders
* Each namespace only one form allowed
* Release every 1 namespace as public github repository
* Start at the lowest place in your codebase
* Release without readme or documentation or tests - just a name
* Release every day 1 library
* Each libgdx lib function brings in the whole framework !!!!
& Can port functions only from outside one package at a time
8 each package needs to have one clojure lib ?

* Remove all '!'
  => everything that mutates returns its object
  => everything can be changed later

  & EVERYTHING IS ONE SPECIFIC ELEMENT WHICH IS WORTHY
  & THAT MEANS EVERYTHING IS A SEPERATE OBJECT
  & THAT MEANS CONSTANTS IS OK IN CODE ITS JUST SOMETHING CONSTANT
  & WHICH IS A SEPARATE OBJECT PROPERTY PROJECT


  Mathematisch gesehen sind seiteneffekte irrelevant
  da man sie mathematisch gesehen nicht beobatchten kann
