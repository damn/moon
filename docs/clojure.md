# clojure folder top level reasoning:

* everything is a clojure 'concept'
* all concepts namings need to make sense together

    -> and shortest possible names
    e.g. com.badlogic.gdx facade
    clojure.com.badlogic.gdx
    'clojure.gdx' applies nicely here
    'or just gdx'
    so the top level folders are needed?
    its not clojure but whatever it is ?
