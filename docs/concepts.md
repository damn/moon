# Du kannst nur fertig werden wenn die Konzepte exact definiert sind.

Start with your first concept: the first - 'namespace' -

'clojure.grid2d/' folder and namespace.

=> What is a grid2d???
    see clojure.grid2d/create-grid
        => its a vector width of vectors height
        => 2 dimensional array/matrix?
        => matrix2 ???

        => THEN:
        'grid2d.adjacent-wall-positions'
            => SUPER KOMPLEX
            => INLINE !!!!!
            => ITS NOT DEFINING ANY MEANINGFUL UNDERSTANDABLE CONCEPT


            Secondly:

            * why no nads allowed / fix nads ?
                => show why

                => pull in your main concepts
                => and extract _each_ concept starting from the lowest level
                => as s separate library
                => then you have the 'language in your mind
                => which you dont need to be converned with anymore
                => as its not in the project anymore
                => so the language gets bigger (libs )
                => and your application gets more and more focused
                => by having less files

                => so just create libraries and release them describe them or whatever

                => create your own language
                => you can name it whatever you want
                => maybe 'damn'
                damn.input
                damn.sprite-batch
                damn.batch
                damn.orthographic-camera
                damn.add-listener
                damn.get-property
                damn.fix-nads
                damn.height
                damn.mapgrid_to_vectorgrid

                EACH FUNCTION MAIN LEVEL 'clojure'
                EACH FUNCTION SEPARATE LIBRARY PUBIC
                EACH FUNCTION DEPEND ONLY ON WHAT IT NEEDS

                BECAUSE YOU NEED TO FIX IN ON A PROBLEM TO SOLVE IT
                OTHERWISE YOU ONLY CREATE PROBLEMS (COUPLING*

                THE RIGHT LANGUAGE FOR THE JOB

                A TOOL NEEDS TO BE DESCRIBED PROPERLY TO UNDERSTAND IT


                ALL IN ONE LEVEL THEN IS THE ULTIMATE LAGNAGUE

                ONe FORM EACH THING ONLY

                1000 libraries

                super specific

                _ AND A PICTURE GENERATED BY AI _
