# Current Task:

* Do NOT leave this window until you have finished the task.

git status is clear? yes

* Problem: Too many floating top-level clojure namespaces lying around.


=> Move every file in its folder where it belongs

& clojure.add-upd-label -> does too much -> move to main-window/dev-menu

KEEP FORMS! ONLY NAMING!
