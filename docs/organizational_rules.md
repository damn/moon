# Goal - Namespaces/Names fix

* Each namespace only one form

* Functions go where - or only names ? namespaces are about the names only

* actions is on txs-fn-map -> its actually a k-fn-map - now we have a concept
    ;  but the concept is already named txs-fn-map so just keep it?

    = and when I have everything in folders I can remove 'clojure' prefix again ????
    madness

    * refer names so short can make blue later if I want and see if globally minimal unique
        => develop a langauge .

        => namespace need to be greppable (com.badlogic.gdx mismatch same as import - but can find for each file)
