Otherwise its all clojure
then the names make sense

 why 'gdx' ???
 or 'moon'
 ; or anything ???

 ; => move also 'com.badlogic.gdx' back and just call it 'clojure.gdx'
 ; => and remove scene2d/etc
  ; you can trim concepts as needed (even remove 'g2d' concept )
  ; but then its not stable

  ; and 'gdx' is just clojure now, only at the moment implemented by
  ; 'clojure.gdx'
  ; e.g. we have the concept of a 'clojure.tiled-map'
  ; or 'clojure.tiled.map'
  ; or 'clojure.tiled' only

  ; just move everything into clojure
