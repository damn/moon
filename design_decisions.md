* monorepo - & dont touch libgdx
    => but this is for making fast changes without `lein install` -> onyl reason
        => also easy grepping
            => don't support library or game engine but make a game ...

* layers and not external libraries
    (layer exactly for my use-case, if I make more games then libs )

    (but a _layer_ _is_ an external library as it has different project.clj dependencies and as such ... ) ?
