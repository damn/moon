We want to release libs under stable names so they do not change anymore
So global names - thus choose 'clojure' as it is the language itself]

clojure.math.v2 (depends on clojure.math)

We can change those global names  again for sure.

But it is a more stable base.

So gdx -> clojure.gdx.*

clojure.gdx == com.badlogic.gdx.gdx


=> IT IS STILL ONE CLASS THERE is no global layers

it is class by class

JAVA - com.badlogic.gdx.Audio
 FFI facade =  com.badlogic.gdx.audio
  clojure API for this _class_ (or mutable container?) = clojure.gdx.audio


  ' everything global name under clojure '
    -> standardised names evolve
    ' clojure.com.badlogic.gdx '

    => flattened structure ??? its not APAI to that class (import) anymore
    but to functions and a 'name'

    this is the win

    clojure.gdx - clojure game development extension
      (no mention of libgdx)
